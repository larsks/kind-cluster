apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd

resources:
  - ../../base
  - resources/clusters

components:
  - ../../components/web-terminal

generatorOptions:
  disableNameSuffixHash: true
